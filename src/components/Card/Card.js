import * as React from 'react';
import'Card.style.css';

const Card = ({}) => {
  return(<div>)
    <picture>
      <img src= {character.image}/>
      <picture>
        <h3>{character.name</h3>
        <p> species: <span>{ character.species} </span>
        </p>
      
}